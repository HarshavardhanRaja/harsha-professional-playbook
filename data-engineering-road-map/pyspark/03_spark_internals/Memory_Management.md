# Memory Management

Spark memory is used for execution, storage, and overhead.

## Key Areas

- Execution memory: joins, aggregations, shuffles, and sorts.
- Storage memory: cached data.
- Overhead memory: JVM, Python worker, and native memory needs.
