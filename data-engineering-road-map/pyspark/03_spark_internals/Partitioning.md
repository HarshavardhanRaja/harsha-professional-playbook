# Partitioning

Partitions are units of parallelism in Spark.

Good partitioning improves parallel execution and reduces skew. Poor partitioning can cause slow tasks, unnecessary shuffles, or small-file problems.
